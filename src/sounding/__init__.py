"""sounding — governance for agent instructions.

Covers MCP servers, Agent Skills, and prompts: audit, fix, pin, scan, eval —
and runs as an MCP server itself.
"""

__version__ = "0.9.4"
MARK = "·)))"
